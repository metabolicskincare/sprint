import SwiftUI

enum Theme {
    static let background = Color.black
    static let word = Color(white: 0.95)
    static let pivot = Color(red: 0.91, green: 0.18, blue: 0.16)
    static let dim = Color(white: 0.42)
    static let guideLine = Color(white: 0.26)
    static let controlSurface = Color(white: 0.09)
}
